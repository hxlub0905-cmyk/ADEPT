# F100：畫布橫躺在上面，工作台在下面（2026-09-08）

**狀態：已收斂（2026-09-09）。** 2026-09-09 搬進 `docs/history/plans/`；之後不再改。

2026-09-08 的外部 UI/UX 評審（F99）對版面配置的結論，使用者定調「版面按照你的
建議改」。這一份寫的是**為什麼要動結構、動成什麼樣、代價是什麼、怎麼驗收**。

## 1. 三個結構性的毛病

現在（U5 之後）的版面是三欄：左邊卡片庫、中間一欄上畫布下設定區、右邊一欄
預覽影像。

```
現在（Tune）
┌─庫─┬──畫布（窄、矮）──┬──預覽影像（大）──┐
│    ├──────────────────┤                  │
│    │ 設定區 │ 儀表板  │  Verdict         │
└────┴──────────────────┴──────────────────┘
```

三個毛病都不是調數字調得掉的：

* **pipeline 是橫著流的，畫布卻是一格窄的直立空間。** 六張卡橫排要 1,900 px
  寬，中欄只有 580 px，所以永遠得縮到 50%——而 50% 在 `_LOD_TERSE`（0.55）之下，
  卡片只剩標題。這是「卡片讀不到字」的根本原因，不是 LOD 門檻的問題。
* **畫布和設定區搶同一欄的高度。** 選一張卡設定區攤開，畫布剩 280 px（1366×768
  上 230 px，GLV 卡本人在畫面外）。兩個都是使用者要看的東西，卻綁在同一根
  splitter 上互相擠。
* **右欄的預覽影像有硬的最小寬度，而且 Build 模式也不讓。** 載入 recipe 選到
  GLV 卡之後 `minimumSizeHint` 變成 1,334 寬、視窗實際 1,495——比 1366 那台
  機器的螢幕還寬。U1 的 `fit_screen` 守的是開窗那一刻，沒有守住載入之後。
  而 Build 模式只把中欄的設定區收掉，預覽仍然吃一半螢幕，畫布還是螢幕上第三大
  的東西（U5 自己的說明就這樣寫）。

## 2. 新版面

```
┌─庫─┬──────────── 畫布（全寬、橫流）────────────┐
│    │  [Load]→[ROI]→[ROI]→[ROI]→[GLV]→[Report]  │
│    ├───────────────┬──────────────┬─────────────┤
│    │ 設定區        │ 儀表板       │ 預覽影像    │
│    ├───────────────┴──────────────┴─────────────┤
│    │ Verdict  more than one box is off · score 10.42 · Path: …
└────┴────────────────────────────────────────────┘
```

* **畫布吃滿卡片庫右邊的整個寬度**，高度由一根橫向 splitter 決定。1366 寬時
  六張卡在 70% 縮放排得下一排。
* **工作台三格並排**：設定區｜儀表板｜預覽影像。設定區、儀表板、影像在同一條
  水平線上——這正是 U8「儀表挨著參數」的決定，而且影像不再跟畫布搶高度。
* **Verdict 是一條常駐的結論列**，橫跨在工作台下面、Problems 列上面。它不在
  splitter 裡，所以 Build 模式收掉工作台之後它還在。P1-2（預覽停在選到的卡時
  說一句為什麼）的那句話也住在這裡。
* **Build 模式**＝工作台收到 0、畫布 fit。這才是「看全貌」。
* **Tune 模式**＝工作台開著（**開窗就開著**，因為影像住在裡面），畫布保底
  `CANVAS_MIN_PX`（200 px，768 高的螢幕上約 35%）、預設 40%。
* **卡片庫維持左邊的窄軌。** 從 Build 切到 Tune 時把卡片區收成只剩 rail
  （58 px），工作台拿到那 200 px；rail 上點一下就回來。開窗時**不**收——
  空白狀態下卡片清單是使用者第一眼要看的東西。

這個形狀就是 n8n 節點詳情面板換個方向：n8n 是「輸入｜參數｜輸出」三格，這裡是
「參數｜量出來的數字｜影像」三格。

## 3. 兩條不走的路

* **n8n 那種全螢幕畫布加右側滑出面板。** 那在 n8n 成立是因為資料預覽就在面板
  裡。這裡使用者一邊拖滑桿一邊看影像，面板滑出來蓋住影像，迴圈就斷了。
* **只調數字不改結構**（Tune 改成畫布 3 設定 2、預覽最小寬度降到 300）。可以讓
  1366 裝得下，但畫布還是窄的直立格，卡片還是要縮。止痛不是治病。

## 4. 做法

* 版面模式的邏輯搬進**新模組 `ui/workbench.py`**（`WorkbenchLayout`），
  `studio.py` 只留建 widget 與轉呼叫——`CLAUDE.md` §4 那條「新的面板一律開新
  模組」，而 `test_size_ceilings.py` 的 7,444 不往上。
* 名字**盡量不改**，讓既有測試改最少：`canvas_column` 仍是「畫布在上」的直向
  splitter（`widget(0)` 仍是畫布，`canvas.py::_build_header` 靠著它）；
  `params_row` 仍是 `stack` ＋ `gauge_pane` 那根橫向 splitter，只是多了第三格
  `preview_pane`，別名 `workbench`。`root_splitter` 從三欄變兩欄：
  `[library, main_column]`。
* `params_open` 的意思從「設定區攤開」變成「**工作台攤開**」。Tune 模式開窗就
  是 True；`set_params_open(False)` 仍然把工作台收到 0（那是使用者手動收的）。
  選到卡片會把收起來的工作台再打開（`_sync_params_pane`），**取消選取不再自動
  收起來**——影像住在裡面，收掉它等於把影像藏起來。
* QSettings：`ui/columns` 從三個值變兩個（舊值長度對不上會自動退回預設）；
  Tune 的畫布/工作台比例換**新的一格** `ui/canvas_split_tune_f100`——舊那一格
  存的是「畫布 2 / 設定 3」，套到新版面上畫布會低於保底。工作台三格的寬度存在
  `ui/workbench_columns`。
* 分數直方圖早就住在 Results 視窗，這一輪不動。

## 5. 要重寫的測試

`test_ui_layout_modes.py`（U5/U8 的座標測試）、`test_ui_f8_ui_polish.py` 的
兩條版面測試、`test_ui_results.py::test_main_window_keeps_only_the_editing_surface`
與 `test_preview_gets_the_widest_column`、`test_ui_studio_m5.py` 的三欄斷言。
每一條改的都是**斷言的形狀**，不是它問的問題：畫布還是只有一份、切模式不重建、
儀表還是挨著參數、影像還是有自己的一格。

新的一條（F99 P2-7 的雛形）：在 1366×768 開視窗、載 recipe、選 GLV 卡之後，
視窗的 `minimumSizeHint().width()` ≤ 1366、畫布高度 ≥ 主欄的 35%。那是這一輪
的驗收，也是 F99 P0-3 的關門。

## 6. 代價，講在前面

* 使用者調過的 Tune 比例會重設一次（換了 QSettings 鍵）。
* 卡片庫在 Tune 模式下會被收成 rail——一次，而且只在從 Build 切過來的時候。
* 兩個模式的 splitter 記憶方向沒變（都是直向），但預設值變了。

## 7. v2（同一天，2026-09-09）：影像回到右欄

第一版做完在 1366×768 上截圖，使用者問了兩句：「畫面會不會太擠」、
「畫布會需要那麼大的空間嗎」。答案是不需要——**畫布要的是寬度，不是面積**，
一排卡只要 200 多 px 高；而且在 Tune 裡它是**導覽**（我在哪張卡、接了什麼），
要看全貌有 Build。§2 把「畫布要橫」推成了「畫布要全寬」，那是兩件事。

第一版的代價全落在最高頻的迴圈上。調參數是「改一格 → 看影像、看數字」，那一刻
要同時看到的三樣東西（設定區、儀表、影像）在 1366 上全都小了：影像 330×250、
儀表 247 px 寬（三張直方圖標題全省略）、設定區出現水平捲軸。

```
v2
┌─庫─┬──────── 畫布（導覽，一排卡的高度）────────┬── 預覽影像（右欄、全高）──┐
│rail├───────────────────┬───────────────────────┤  Image stream ▾            │
│    │ 設定區             │ 儀表板                 │  ImageView（大）           │
│    │                   │                       │  Verdict · score · Path   │
└────┴───────────────────┴───────────────────────┴───────────────────────────┘
Build：右欄與工作台都收到 0，畫布吃滿。
```

| 1366×768 | 第一版 | v2 |
|---|---|---|
| Tune 的畫布 | 1100×250，六張卡 57% | 約 800×250，42%（Build 仍 100% 寬） |
| 影像 | 330×250 | 約 440×600 |
| 設定區、儀表 | 380 / 250 寬 | 各約 400 寬 |
| Verdict | 底部常駐列 | 右欄影像下面（跟這一顆的圖挨著；Build 跟右欄一起收） |

形狀上這是回到三欄，但舊三欄的兩個病不會回來：右欄的硬最小寬度已經拆掉
（§1 第三點的病根：空白狀態那一頁、游標讀數），卡片庫在 Tune 會收成 rail。
舊版面的畫布 470 px，v2 是 800。

**「最好」還沒定案**：Tune 裡花多少時間看畫布、看影像多還是看直方圖多，這兩件
事我從文件推不出來，要在那台 1366 的機器上用過才知道。v1 在 `git log` 裡
（commit 17ad451），試完挑一個——不要做成設定讓兩種都留著（使用者自己的規矩：
兩套準則使用者反而不知道怎麼用）。

QSettings 又多一格 `ui/columns_f100v2`（root 三欄），只在 Tune 存。

### 7.1 兩個在 v2 上量到、跟版面本身無關的病

* **`_save_sizes` 在 pytest 裡照寫。** `_load_sizes` 在測試裡不讀（那是它自己的
  規矩），但寫的那一半沒有守 —— 於是每一條會關窗的測試都把它的欄寬寫進開發者
  真正的 `Studio.ini`，包括一份在 show 之前就關掉的視窗存出的 ``31,31,30``。
  下一次手動開 Studio 讀到它，Qt 按比例放大，右欄 607、中欄 348。兩個修法都做：
  寫的那一半也守 pytest；讀回來的每一欄要 ≥ 100 px 才算一欄。
* **右欄的最小寬度以前被 Verdict 那一列撐到 511**：膠囊的最小寬度跟著類別名
  走（F99 P0-4）＋ score ＋ 那句為什麼 ＋ 路徑排成一行。右欄只有四百多 px，
  所以改成三行（膠囊、數字與說明、路徑）。同一類的還有 GLV 那排 intent 膠囊
  （HBox → `_ChipFlow`，設定區最小寬度 376 → 187）與接線格的說明（窄的時候
  先讓，不撐格子）。

## 8. v3（同一天）：儀表換到影像下面，其他回到最一開始的排法

v2 做完，使用者看著直方圖那一格說「給的空間會不會太少」——會：250×370，三張
直方圖標題全省略、橫軸沒刻度。然後說「老實說我還是覺得最一開始的排版最好，
儀表換到影像下方」。

他是對的，而且比 v2 好，理由算得出來：**直方圖要的是寬度**（橫軸、標題、逐框
的點帶都是橫的），**設定區要的也是寬度**（滑桿），v2 讓這兩個要寬的東西擠同一
列。v3 各給一整欄的寬：設定區吃滿中欄（約 790）、儀表吃右欄（約 440）。
只有影像付出高度（470 → 約 280），而它是三樣東西裡最能靠拖一下分隔線補回來的。

```
v3
┌─庫─┬──────── 畫布（導覽，一排卡的高度）────────┬── 預覽影像 ─────────┐
│rail│                                           │  Verdict · score    │
│    ├───────────────────────────────────────────┼─────────────────────┤
│    │ 設定區（吃滿中欄）                          │  儀表板（直方圖）    │
└────┴───────────────────────────────────────────┴─────────────────────┘
右欄上下是一根 splitter（影像預設 55%），拖過記得住；Build 收右欄與工作台。
```

| 1366×768 | v2 | v3 |
|---|---|---|
| 設定區 | 390 寬 | 約 790 寬 |
| 儀表 | 250 × 370 | 約 440 × 250 |
| 影像 | 440 × 470 | 440 × 約 280（拖得回來） |
| U8「儀表挨著參數」 | 同一根 splitter 左右兩格 | 中欄下半與右欄下半，左右相鄰，同一條視線 |

這也是使用者用了兩個月的排法：我在這裡量的是幾何，他量的是手感，而後者
在「最好」這件事上分量更重。U8 那條測試改成「兩根 splitter 的下半格水平相
鄰」，精神沒變。QSettings 多一格 `ui/right_split_f100v3`。

### 8.1 細線（2026-09-09）

使用者看 v3 截圖：「分隔線看起來又怪怪的」。量像素：直向 5px、橫向 **9px**
的實心灰條 —— v2 那條 QSS（`width: 5px; margin: 0 2px`）想做「看得見 1px、
抓得到 5px」，Qt 沒照做。離線探針試了 `margin`、`border-left/right`、四邊
`border`、硬邊漸層四種：Qt 算把手粗細時不分方向、畫的時候才分，
`:horizontal` 與 `:vertical` 的尺寸互相干擾；漸層則被反鋸齒糊掉。沒有一種在
兩個方向都給出 5px／1px。

所以把手自己畫：`ui/splitters.py` 的 `HairlineSplitter`，`paintEvent` 在把手
正中畫 1px 的 `divider`（滑過換 `border_hover`），顏色畫的時候才從
`theme.TOKENS` 取。QSS 只剩 `background: transparent`。七個呼叫點
（`studio.py` 四、`results.py` 二、`template_dialog.py` 一）全換；
`tests/test_ui_splitters.py` 開一支、抓圖、數像素（兩個方向 × 兩個主題），
並擋住 `d4t/ui` 再出現裸的 `QSplitter(`。
